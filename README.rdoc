= timeentry_importer

This is a plugin for Redmine that allows import time entries form a csv file.

Is based on Issue Import feature, adding field customization in ordr to import time entries

CSV file structure:

issue, date, spent hours, comment, activity, user

if user is empty time entries will be associated with user who is running the import
